# حزمة كود لعبة «فكّر بسرعة» للمراجعة الأمنية

هذه الحزمة تحتوي على ملفات المصدر اللازمة لمراجعة كود اللعبة، بما في ذلك واجهة React، منطق اللعبة والاقتصاد، تكامل الإعلانات، كود Android الأصلي، الاختبارات، إعدادات البناء، وملفات التوثيق.

## الملفات الموجودة

| القسم | المحتوى |
|---|---|
| `client/` | واجهة اللعبة ومكوّنات React وملفات التصميم |
| `server/` | اختبارات اللعبة وخدمات المشروع وملفات الخادم |
| `shared/` | الأنواع والثوابت المشتركة |
| `android/` | مشروع Capacitor/Android وكود Unity Ads وملفات Manifest والإعدادات |
| `drizzle/` | مخطط قاعدة البيانات وملفات الترحيل |
| `scripts/` | أدوات بناء وفحص بنك الأسئلة وتهيئة بيانات المتجر |
| `*.test.*` | اختبارات Vitest واختبارات منطق الإعلانات والاقتصاد والتوقيع |
| `ANDROID_*.md` | توثيق إعدادات AdMob وUnity Ads وبناء Android |

## ملفات مستبعدة عمدًا

تم استبعاد `android/tahaddi-release.jks` لأنه مفتاح توقيع خاص، كما تم استبعاد `.project-config.json` وملفات `.env` وملفات الشهادات والمفاتيح لأنها قد تحتوي على أسرار أو بيانات اتصال. وتم استبعاد `node_modules` وملفات `dist` و`android/app/build` وملفات APK/AAB لأنها نواتج بناء وليست كودًا مصدرًا.

لا تُرسل مفتاح التوقيع الخاص أو كلمات مرور التوقيع إلى أي جهة تقوم بالمراجعة. إذا احتاج المراجع إلى إعادة البناء، استخدم مفتاحًا منفصلًا مخصصًا للمراجعة، ولا تستخدم مفتاح الإصدار الحقيقي.

## نقاط مقترحة للفحص

راجع التحقق من المكافآت داخل الإعلانات، عدم منح العملات قبل إشارة اكتمال الإعلان، تخزين الرصيد محليًا، أي أسرار مضمّنة داخل التطبيق، صلاحيات Android، النقل الشبكي، روابط WebView/Capacitor، والتحقق من المدخلات في الخادم. تذكّر أن أي مفتاح موجود داخل تطبيق Android يمكن استخراجه من الحزمة، ولذلك يجب اعتبار معرفات الإعلانات العامة مختلفة عن الأسرار.
