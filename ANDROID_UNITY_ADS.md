# Unity Ads داخل تطبيق «فكّر بسرعة»

تمت إضافة Unity Ads إلى نسخة Android الأصلية مع Game ID الخاص بتطبيق Android: `800364136`.

| نوع الإعلان | Placement ID | الاستخدام داخل التطبيق |
|---|---|---|
| Banner | `Banner_Android` | يظهر كبديل في شاشة النتيجة عند تعذر بانر AdMob، ولا يظهر أثناء الأسئلة. |
| Interstitial | `Interstitial_Android` | متاح عبر طبقة Android الأصلية للاستخدام بين الجولات عند الحاجة. |
| Rewarded | `Rewarded_Android` | بديل لإعلان التلميح؛ لا تُمنح المكافأة إلا بعد اكتمال المشاهدة. |

تبدأ Unity Ads عند فتح Activity الرئيسية. في نسخة اختبار AdMob، يستخدم Debug App ID `ca-app-pub-3940256099942544~3347511713`، ووحدة Banner `ca-app-pub-3940256099942544/9214589741` ووحدة Rewarded `ca-app-pub-3940256099942544/5224354917`؛ أما Release فيستخدم معرفات AdMob الحقيقية المسجلة للتطبيق. يضبط Gradle قيمة `BuildConfig.UNITY_ADS_TEST_MODE` حسب نوع البناء: نسخة **Debug** تستخدم `true` لاختبار الإعلانات، بينما نسختا **Release** تستخدمان `false` ولا تُرسلان إلى المتجر بوضع الاختبار. يحتوي المشروع أيضًا على تكامل AdMob السابق؛ عند فشل تحميل AdMob، تحاول طبقة الإعلانات استخدام Unity Ads كبديل للبانر أو للإعلان المكافأ.

## فصل ملفات الاختبار عن ملفات النشر

ملف الاختبار المباشر هو `android/app/build/outputs/apk/debug/fakkar-besoraa-testmode.apk`، وقيمته الفعلية `UNITY_ADS_TEST_MODE = true`. لا ترفع هذا الملف إلى Google Play أو Samsung Galaxy Store؛ استخدمه فقط للتثبيت اليدوي والتحقق من ظهور Banner وRewarded.

ملفات النشر هي `android/app/build/outputs/apk/release/fakkar-besoraa-release.apk` للتثبيت المباشر و`android/app/build/outputs/bundle/release/fakkar-besoraa-release.aab` للرفع إلى المتاجر، وقيمتهما الفعلية `UNITY_ADS_TEST_MODE = false`. يجب استخدام ملفات Release فقط في التسليم العام، مع إبقاء اختبار الإعلانات الفعلي على جهاز Android وبحسابات ومنصات الإعلانات المهيأة.

## اقتصاد العملات داخل اللعبة

تمنح اللعبة عملة واحدة فقط عند تسجيل إجابة فعلية، ولا تمنح عملة عند انتهاء الوقت تلقائيًا. يمكن للاعب مشاهدة فيديو إعلان مكافأ للحصول على 25 عملة إضافية، ولا تُضاف هذه العملات إلا عندما يعيد مسار الإعلان نتيجة `rewarded` التي تعني اكتمال المشاهدة؛ الفشل أو `no-fill` أو `sdk-error` أو الإغلاق المبكر لا يضيف أي عملات. تُستخدم العملات حصريًا لشراء مساعدات الأسئلة: حذف إجابتين مقابل 60 عملة أو إضافة 10 ثوانٍ مقابل 40 عملة، ولا توجد وسيلة لشراء أيام أو إزالة الإعلانات بالعملات.

## اختبار قبل النشر

ثبّت APK الاختبار أو Release على هاتف Android حقيقي، وتأكد من اتصال الإنترنت. افتح جولة، وجرّب زر التلميح، وأكمل إعلان Rewarded كاملًا؛ يجب أن تختفي إجابتان خاطئتان فقط بعد اكتمال الإعلان. بعد نهاية الجولة والوصول إلى شاشة النتيجة، تحقق من ظهور بانر واحد فقط؛ البانر لا يُستدعى أثناء شاشة السؤال. تأكيد الظهور الفعلي للبانر يتطلب جهاز Android حقيقيًا، ولا يمكن إثباته من الاختبارات البرمجية وحدها. لا تعتمد على ظهور إعلان فوري في كل مرة، لأن توفر الإعلانات يتأثر بالملء والبلد والحساب وحالة الموافقة.

تأكد من أن الوحدات الثلاث في لوحة Unity ما زالت Active، وأن إعدادات الخصوصية والموافقة على الإعلانات مطبقة بما يناسب البلدان المستهدفة قبل الإطلاق العام.

## مراجع Unity الرسمية

- [تثبيت Unity Ads SDK لأندرويد](https://docs.unity.com/en-us/grow/ads/android-sdk/install-sdk)
- [الإعلانات المكافأة في Android](https://docs.unity.com/en-us/grow/ads/android-sdk/rewarded-ads)
- [إعلانات Banner في Android](https://docs.unity.com/en-us/grow/ads/android-sdk/banner-ads)
