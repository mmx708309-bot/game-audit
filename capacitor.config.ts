import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.tahaddi.quiz50k",
  appName: "فكّر بسرعة",
  webDir: "dist/public",
  android: { allowMixedContent: false },
};

export default config;
