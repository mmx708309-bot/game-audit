# إعداد Android وAdMob

تُغلف لعبة **فكّر بسرعة** باستخدام Capacitor لتعمل كتطبيق Android. يظهر إعلان البانر الأصلي في **شاشة النتيجة فقط**؛ ولا يظهر داخل شاشة السؤال أو أثناء عدّ الوقت.

| الإعداد | القيمة |
|---|---|
| اسم التطبيق | فكّر بسرعة |
| معرّف حزمة Android | `com.tahaddi.quiz50k` |
| معرّف تطبيق AdMob | `ca-app-pub-5675767725733870~8581665731` |
| وحدة البانر | `ca-app-pub-5675767725733870/9902486260` |
| وحدة الإعلان المكافأ | `ca-app-pub-5675767725733870/4071575086` |

يطلب التطبيق حالة موافقة الإعلانات قبل طلب الإعلان، ثم يعرض بانرًا متكيفًا أسفل شاشة النتيجة على Android.

يستطيع اللاعب اختيار زر **«شاهد إعلان وخد تلميح»** داخل السؤال. لا يُمنح التلميح إلا عندما يؤكد المكوّن الأصلي اكتمال إعلان المكافأة، ويستبعد التلميح إجابتين خاطئتين من دون كشف الإجابة الصحيحة أو إيقاف الوقت.

يُضاف معرّف تطبيق AdMob في `AndroidManifest.xml`، بينما تُمرر معرّفات وحدتي البانر والمكافأة برمجيًا إلى مكوّن AdMob عند العرض، وفق نموذج التكامل الخاص بالمكوّن.

## بناء Android

1. نفّذ `pnpm build` ثم `pnpm exec cap sync android` بعد أي تعديل للواجهة.
2. نفّذ `cd android && ./gradlew assembleDebug` لإنتاج نسخة اختبار في `app/build/outputs/apk/debug/`.
3. لنسخة متجر موقّعة، أنشئ مفتاح توقيع خاصًا بك واضبط إعدادات `signingConfigs` محليًا؛ لا تحفظ ملف المفتاح أو كلمات المرور داخل المشروع.

تم التحقق من بناء نسخة Android التجريبية بنجاح باستخدام Android API 36 وJava 21. يوجد ملف APK التجريبي الناتج في:

`android/app/build/outputs/apk/debug/app-debug.apk`

## مرجع

يستخدم التكامل مكوّن `@capacitor-community/admob` وخطوات تعريف تطبيق AdMob في بيان Android كما وردت في [دليل إعلانات Capacitor الرسمي](https://capacitorjs.com/docs/guides/ads) و[توثيق مكوّن AdMob](https://github.com/capacitor-community/admob).
